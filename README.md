# AI-Bear: Интеллектуальный Telegram-бот для консультирования клиентов BAHUR

## 📋 Описание проекта

**AI-Bear** — это профессиональный Telegram-бот на базе FastAPI и DeepSeek LLM, разработанный для автоматизации консультирования клиентов интернет-магазина парфюмерных масел BAHUR. Бот обеспечивает круглосуточную поддержку клиентов, предоставляет релевантную информацию о продукции и способствует увеличению продаж через интеллектуальные рекомендации.

## 🚀 Эволюция проекта

Проект начал свою эволюцию как интеллектуальный менеджер по продажам, прошедший несколько ключевых этапов развития:

### **1. Фундамент: Векторный поиск и RAG-архитектура**
- **Подготовка данных:** Бизнес-материалы были разбиты на семантические фрагменты (чанки)
- **Векторизация:** Для преобразования текста в эмбеддинги использовалась sentence-transformers модель, для которой был выделен выделенный сервер
- **Хранилище:** Созданные эмбеддинги загружались в облачную векторную базу данных Qdrant

### **2. Механизм работы первоначальной версии:**
```
Запрос клиента → Поиск по косинусному расстоянию → Извлечение релевантных данных → 
→ Генерация ответа DeepSeek LLM → Формирование финального ответа
```
- Интеллектуальная система промптов объединяла контекст запроса, найденную информацию и бизнес-логику
- Генерация ответов происходила с учетом релевантных данных из векторного хранилища

### **3. Голосовые коммуникации:**
Первоначально система поддерживала телефонные продажи через:
- **SST (Speech-to-Text):** Автоматическое распознавание голосовых запросов
- **TTS (Text-to-Speech):** Синтез естественной речи для ответов
- **Технология:** Интеграция с Yandex Speechkit для обработки голосовых взаимодействий

### **4. Трансформация в Telegram-бота:**
В соответствии с изменением потребностей заказчика:
- Голосовые функции были консолидированы
- Основной акцент смещен на текстовое взаимодействие
- Система мигрировала в Telegram-экосистему, сохранив всю интеллектуальную логику

Этот путь развития позволил создать гибкую систему, способную адаптироваться под меняющиеся бизнес-требования, сохраняя при этом мощную интеллектуальную основу.
В дальнейшем, в соответствии с потребностями заказчика TTS был упразднён и менеджер продаж стал работать через Telegram бота

## 🎯 Решаемые задачи

### 24/7 Консультирование клиентов
- **Непрерывная работа**: Бот доступен круглосуточно без перерывов и выходных
- **Мгновенные ответы**: Асинхронная архитектура обеспечивает быструю обработку запросов
- **Масштабируемость**: FastAPI позволяет обрабатывать множество одновременных запросов

### Релевантные продажи
- **Умный поиск по нотам**: Автоматическое распознавание запросов о парфюмерных нотах и мгновенный поиск соответствующих ароматов
- **Контекстные рекомендации**: LLM анализирует запросы клиентов и предлагает подходящие товары из каталога

### Клиентоориентированность
- **Персонализированный опыт**: Сохранение состояний пользователей для непрерывного диалога
- **Множественные режимы взаимодействия**: Поддержка текстовых сообщений, голосовых запросов и интерактивных кнопок
- **Дружелюбный интерфейс**: Эмодзи, понятные инструкции и интуитивная навигация

## 🏗️ Архитектура системы

### Технологический стек

**Backend Framework:**
- **FastAPI** — современный асинхронный веб-фреймворк для высокопроизводительных API
- **Uvicorn** — ASGI-сервер для запуска приложения

**LLM Integration:**
- **DeepSeek Chat API** — интеграция с продвинутой языковой моделью для генерации ответов
- **Temperature: 0.5** — баланс между креативностью и точностью ответов

**Telegram Integration:**
- **Telegram Bot API** — полноценная интеграция с мессенджером
- **Webhook-based** — надежный механизм получения обновлений

**Дополнительные библиотеки:**
- **aiohttp/httpx** — асинхронные HTTP-клиенты для внешних API
- **SpeechRecognition + pydub** — обработка голосовых сообщений
- **Pydantic** — валидация данных и модели запросов

### Архитектурные решения

#### 1. Асинхронная обработка запросов
```python
# Все операции выполняются асинхронно для максимальной производительности
async def ask_deepseek(question):
    async with aiohttp.ClientSession(timeout=timeout) as session:
        async with session.post(url, headers=headers, json=data) as resp:
            # Обработка ответа
```

#### 2. Персистентное хранение состояний
- Состояния пользователей сохраняются в `user_states.json`
- Автоматическая загрузка при старте приложения
- Сохранение после каждого изменения состояния
- Поддержка двух режимов: `awaiting_ai_question` и `awaiting_note_search`

#### 3. Многоуровневая обработка ошибок
- Глобальный обработчик исключений на уровне FastAPI
- Try-catch блоки в каждой критической функции
- Детальное логирование всех операций
- Graceful degradation при ошибках API

## 🤖 Система промптов и LLM интеграция

### Ключевые особенности промптов

1. **Контекстная база знаний**: Весь каталог BAHUR загружается в промпт (`bahur_data.txt`), обеспечивая актуальную информацию
2. **Строгие правила ответов**: 10 четких правил предотвращают генерацию нерелевантных ответов
3. **Формат ответов**: HTML-форматирование ссылок для автоматического преобразования в кнопки
4. **Клиентоориентированность**: Инструкции по вежливому перенаправлению оффтопика

## 💬 UI/UX особенности

### Интерактивные элементы

#### Главное меню (`/start`, `/menu`)
- **🧸 Ai-Медвежонок** — активация режима консультации с AI
- **🍓 Ноты** — быстрый поиск ароматов по нотам
- **🍦 Прайс** — прямая ссылка на прайс-лист
- **🍿 Магазин** — переход в интернет-магазин
- **♾️ Вопросы** — FAQ и ответы на частые вопросы
- **🎮 Чат** — сообщество в Telegram
- **💎 Статьи** — образовательный контент
- **🏆 Отзывы** — отзывы клиентов

#### Динамические кнопки
- Автоматическое создание кнопок из ссылок в ответах AI
- Кнопки "Подробнее" и "Повторить" для результатов поиска
- Inline-клавиатуры для навигации без отправки сообщений

### UX улучшения

1. **Индикатор печати**: `send_typing_action()` показывает, что бот обрабатывает запрос
2. **Вариативные приветствия**: 5 различных вариантов приветствия для разнообразия
3. **Умная маршрутизация**: Автоматическое определение типа запроса (нота vs вопрос)
4. **Обработка ошибок**: Понятные сообщения об ошибках вместо технических деталей

## 🎤 Обработка голосовых сообщений

### Полноценная поддержка голоса

```python
async def recognize_voice_content(file_content):
    # Конвертация OGG → WAV
    # Распознавание речи через Google Speech Recognition
    # Обработка результата через AI
```

**Возможности:**
- Загрузка голосовых файлов из Telegram
- Конвертация форматов (OGG → WAV)
- Распознавание русской речи через Google Speech Recognition API
- Автоматическая передача распознанного текста в AI для генерации ответа
- Обработка ошибок распознавания с понятными сообщениями

## 🔍 Интеграция с внешними API

### API поиска ароматов

**Эндпоинты:**
- `GET /bahur/search/?text={note}` — поиск по тексту (ноте)
- `GET /bahur/search/?id={aroma_id}` — поиск по ID аромата

**Особенности:**
- Таймауты для предотвращения зависаний (10 секунд)
- Обработка ошибок сети и API
- Структурированные ответы с брендом, названием, описанием и ссылкой

### Обработка результатов поиска

```python
if result.get("status") == "success":
    msg = f'✨ {result.get("brand")} {result.get("aroma")}\n\n{result.get("description")}'
    # Добавление кнопок "Подробнее" и "Повторить"
```

## 📊 Управление состояниями пользователей

### Два режима работы

1. **`awaiting_ai_question`** — режим консультации с AI
   - Пользователь может задавать любые вопросы
   - AI отвечает на основе каталога BAHUR
   - Состояние сохраняется между сообщениями

2. **`awaiting_note_search`** — режим поиска по нотам
   - Пользователь вводит название ноты
   - Система ищет ароматы с этой нотой
   - После поиска состояние сбрасывается

### Персистентность

- Состояния сохраняются в JSON-файл
- Автоматическая загрузка при старте
- Атомарные операции сохранения

## 🛡️ Надежность и обработка ошибок

### Многоуровневая защита

1. **Глобальный обработчик исключений**
   ```python
   @app.exception_handler(Exception)
   async def global_exception_handler(request: Request, exc: Exception):
       logger.error(f"Global exception handler: {exc}")
       return JSONResponse(status_code=500, content={"detail": "Internal server error"})
   ```

2. **Таймауты для всех внешних запросов**
   - DeepSeek API: 30 секунд
   - Search API: 10 секунд
   - Telegram API: 30 секунд

3. **Graceful degradation**
   - При ошибке DeepSeek — понятное сообщение пользователю
   - При ошибке поиска — сообщение "Ничего не найдено"
   - При ошибке голоса — предложение написать текстом

4. **Детальное логирование**
   - Все операции логируются
   - Ошибки с полным traceback
   - Логирование входящих webhook-запросов

## 📝 Логирование и мониторинг

### Структура логов

- **INFO**: Успешные операции, маршрутизация запросов
- **ERROR**: Ошибки API, исключения, проблемы с сетью
- **WARNING**: Неизвестные типы обновлений

### Ключевые точки логирования

- Входящие webhook-запросы
- Обработка сообщений и callback-запросов
- Запросы к DeepSeek API
- Запросы к Search API
- Ошибки обработки голосовых сообщений
- Установка webhook при старте

## 🚀 Развертывание

### Переменные окружения

```bash
TOKEN=<Telegram Bot Token>
WEBHOOK_BASE_URL=<Base URL для webhook>
DEEPSEEK=<DeepSeek API Key>
PORT=8000  # Опционально, по умолчанию 8000
```

### Запуск приложения

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск через uvicorn
uvicorn 1:app --host 0.0.0.0 --port 8000

# Или напрямую
python 1.py
```

### Webhook настройка

При старте приложения автоматически устанавливается webhook:
```
POST https://api.telegram.org/bot{TOKEN}/setWebhook
URL: {WEBHOOK_BASE_URL}/webhook/ai-bear-123456
```

## 📁 Структура проекта

```
ai-bear/
├── main.py                 # Основной файл приложения
├── bahur_data.txt       # Каталог продукции BAHUR (контекст для LLM)
├── requirements.txt     # Зависимости Python
├── user_states.json     # Состояния пользователей (создается автоматически)
└── README.md            # Документация проекта
```

## 🔐 Безопасность

### Реализованные меры

1. **Переменные окружения**: Все секретные ключи хранятся в переменных окружения
2. **Валидация данных**: Pydantic модели для валидации входящих запросов
3. **Обработка исключений**: Предотвращение утечки информации об ошибках
4. **Таймауты**: Защита от зависаний и DoS-атак

## 📈 Метрики и производительность

### Оптимизации

- **Асинхронная обработка**: Все I/O операции асинхронные
- **Кэширование данных**: Каталог загружается один раз при старте
- **Эффективные таймауты**: Баланс между скоростью и надежностью
- **Минимальные зависимости**: Только необходимые библиотеки

**Версия:** 1.0  
**Последнее обновление:** 2025  
**Лицензия:** Proprietary

